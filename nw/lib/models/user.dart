class User {

  User({this.id, this.username, this.password,this.nativelang, this.phoneNumber, this.displayName});

  // user attributes
  int id;
  String username, nativelang, password, phoneNumber, displayName;
}